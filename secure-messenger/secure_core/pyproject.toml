[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "secure_core"
requires-python = ">=3.9"
description = "Rust-ядро криптоопераций для защищённого мессенджера"

[tool.maturin]
features = ["pyo3/extension-module"]
